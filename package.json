{
  "name": "tile-bath-plus-proposal-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check catalog-data.js && node --check server.js && node --check scripts/build-proposal-deck.mjs && node --check scripts/sync-products-to-supabase.mjs && node -e \"JSON.parse(require('fs').readFileSync('data/products.json','utf8'))\""
  }
}

# Copy this file to keystore.properties. The real file is ignored by Git.
# Use an absolute path or a path relative to mobile/android.
storeFile=C:/secure/jajaego-upload.jks
keyAlias=jajaego-upload

# Passwords must only be supplied as environment variables:
# JAJAEGO_KEYSTORE_PASSWORD
# JAJAEGO_KEY_PASSWORD
